// The existing logger cannot provide the required distributed trace correlation.
logger.info('Processing request');

// Introducing a trace mechanism is justified because the existing capability cannot satisfy the requirement.
// LOGCRAFT_TRANSVERSAL: unnecessary-mechanism-safe
