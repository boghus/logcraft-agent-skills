// The project uses a plain logger without structured-field support.
logger.info('Order created for ' + orderId);

// The recommendation must not require an unsupported structured-field API.
// LOGCRAFT_TRANSVERSAL: unsupported-capability
