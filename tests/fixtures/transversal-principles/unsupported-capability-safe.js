// The project explicitly supports structured fields through its logger API.
logger.info('Order created', { orderId });

// A recommendation using that existing capability is compatible with the runtime.
// LOGCRAFT_TRANSVERSAL: unsupported-capability-safe
